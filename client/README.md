# Crypto Trading Dashboard - Client

React-based frontend for the cryptocurrency trading dashboard.

## Environment Setup

Copy `env.example` to `.env` and configure:

```bash
cp env.example .env
```

## Setup

```bash
npm install
npm run dev
```
